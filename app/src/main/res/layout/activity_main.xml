<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".MainActivity">

    <ImageButton
        android:id="@+id/settings_button"
        style="@android:style/Widget.DeviceDefault.ImageButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_settings"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/text_grey"
        android:contentDescription="Settings" />

    <LinearLayout
        android:id="@+id/clock_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView android:id="@+id/hour1" style="@style/DigitStyle" tools:text="1"/>
        <TextView android:id="@+id/hour2" style="@style/DigitStyle" android:layout_marginStart="4dp" tools:text="2"/>

        <TextView style="@style/SeparatorStyle" />

        <TextView android:id="@+id/minute1" style="@style/DigitStyle" tools:text="2"/>
        <TextView android:id="@+id/minute2" style="@style/DigitStyle" android:layout_marginStart="4dp" tools:text="0"/>

        <TextView style="@style/SeparatorStyle" />

        <TextView android:id="@+id/second1" style="@style/DigitStyle" tools:text="3"/>
        <TextView android:id="@+id/second2" style="@style/DigitStyle" android:layout_marginStart="4dp" tools:text="5"/>

    </LinearLayout>

    <TextView
        android:id="@+id/date_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textColor="@color/text_grey"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/clock_container"
        tools:text="July 19, 2025" />

</androidx.constraintlayout.widget.ConstraintLayout>